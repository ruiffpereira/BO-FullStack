# Build Tasks: Shell — submenus · perfil · tema · badge

Generated from: .design/shell-nav-perfil/DESIGN_BRIEF.md
Date: 2026-07-06 · Execução: agentes (Sonnet) + review adversarial, fatia a fatia, commit por fatia.

## Fase 0 — Quick wins (independentes, valor imediato)

- [x] **T0.1 Tema: persistência local + default do sistema**: `App.tsx` — init lazy do tema: `localStorage("bo.theme")` > `prefers-color-scheme` > dark; `toggleTheme` grava no localStorage; script inline no `index.html` aplica a classe `dark` antes do 1.º paint (sem flash) e alinha o `<meta theme-color>`. _Modifica: `src/App.tsx:22-43`, `index.html`. Testes: unit do init (3 caminhos)._ (O lado servidor chega no T3.4 — este slice já resolve "esquece o tema no reload".)
- [x] **T0.2 Badge de mensagens na sidebar**: `NavItem` ganha prop `badge?: number`; o Shell liga `useChatUnread()` ao item `/mensagens` (bolinha com contagem, cap "9+", `aria-label`). Coerente com o `ChatLauncher`; visível também com sidebar colapsada (dot no ícone). _Modifica: `src/components/Shell.tsx:47-62`. Reusa: `useChatUnread` (`src/hooks/useChat.ts:207`)._

## Fase 1 — Fundação da navegação (bloqueia a Fase 2)

- [x] **T1.1 Sidebar com submenus + rotas aninhadas (infra)**: `SUBMENU` map (path pai → subitens `{id,label,path,perm?}`) como fonte única; `NavItem` expansível (acordeão: expande o pai da rota ativa, seta rotação, `aria-expanded`); ativo por prefixo; flyout no modo colapsado (portal, hover/click); drawer mobile herda os submenus. Guard do `Shell.tsx:203-207` passa a prefixo + redirect de tab sem permissão para o 1.º subitem permitido. Rotas aninhadas declaradas no `App.tsx` (uma rota por subpágina; pai redireciona para o 1.º subitem quando aplicável). _Modifica: `Shell.tsx`, `App.tsx:76-93`. Nada muda ainda nas páginas — o Financeiro serve de piloto no T1.2._
- [x] **T1.2 Piloto: Financeiro em paths reais**: `/financeiro`(negócio)·`/financeiro/agenda|loja|ginasio|despesas`; remove as `Tabs` da página; redirects `?vista=X`→path e `/despesas`→`/financeiro/despesas`. Atualiza `notificationTarget.ts` (`payment/reminder`→`/financeiro/ginasio`) e `FirstValueChecklist` (2 hrefs). _Modifica: `FinanceiroPage.tsx`, `App.tsx:82`, `notificationTarget.ts`, `FirstValueChecklist.tsx:123,145`. Testes: `notificationTarget.test.ts` + e2e financeiro/despesas._ **Gate: design-review do padrão visual do submenu antes de replicar.**

## Fase 2 — Migração página a página (independentes entre si; todas dependem do T1.1)

Cada slice: rotas novas + remoção das `Tabs` de topo + redirect `?tab=` + deep-links preservados + unit/e2e/page objects da página migrados.

- [x] **T2.1 Loja**: `/loja/produtos|encomendas|categorias`; `?tab=`→redirect; `?openProduct=` continua; `notificationTarget` (`order`, `stock`) + `FirstValueChecklist` (2 hrefs). _Modifica: `Loja.tsx:411-420,759-761`._
- [x] **T2.2 Clientes**: `/clientes`(lista)·`/clientes/leads`; `?tab=leads&lead=`→`/clientes/leads?lead=`; `?cliente=` mantém-se; tabs da ficha (agenda/ginásio) NÃO migram. _Modifica: `Clientes.tsx:79-85,265-268`; `notificationTarget` (`customer`)._
- [x] **T2.3 Website**: `/website`·`/website/template|paginas|marca|rodape-nav|dominio`. _Modifica: `Website.tsx:78-84,1713`; migrar as ~48 asserções de `tests/unit/Website.test.tsx` que navegam por tabs._
- [x] **T2.4 Admin**: `/admin/utilizadores|permissoes|componentes|tokens|faturacao|integracoes|atividade|sistema` (`/admin`→utilizadores). _Modifica: `Admin.tsx:1557-1601`; e2e admin/admin-tokens._
- [x] **T2.5 Ginásio**: `/ginasio`(exercícios)·`/ginasio/treinos|planos|clientes`; sub-toggle interno fica. Subitem "clientes" rotulado **"Progresso de clientes"** (não "Clientes") — evita colidir com o nome acessível do item core `/clientes` quando o grupo Ginásio está expandido (colisão real nos testes RBAC). _Modifica: `Ginasio.tsx:127,2878-2907`, `App.tsx`, `navigation.ts`, `Conteudos.tsx` (deep-link das associações CMS por tipo), `notificationTarget.ts` (comentário); e2e ginasio/ginasio-detalhe + rbac/rbac-matriz (sem alterações — a colisão já não existe)._
- [x] **T2.6 Conteúdos**: `/conteudos`(site público, âncora)·`/conteudos/produtos|servicos|ginasio|linguas|emails|notificacoes` com gating por permissão por subitem (`SUBMENU["/conteudos"]` com `perm`, espelha o antigo `visibleTabs`). Os 4 primeiros subitens (website/product/service/gym) NÃO puderam manter o nome "óbvio" (Website/Loja/Agenda/Ginásio) — colidiam com os itens homónimos da sidebar, sempre visíveis quando o subitem também está (mesma permissão gate os dois) — renomeados para "Site público"/"Produtos"/"Serviços"/"Ginásio (nomes)" (mesmo padrão do "Progresso de clientes" no Ginásio, T2.5). _Modifica: `Conteudos.tsx` (`CMS_TABS` relabeled + `ConteudosView` exportado + `view` prop substitui `useState`/`Tabs`), `navigation.ts`, `App.tsx`; e2e `conteudos.spec.ts` (submenu + gating limited@e2e) + `ConteudosPage.ts`._
- [x] **T2.7 Agenda**: `/agenda`(calendário)·`/agenda/marcacoes|servicos|config`; `?marcacao=`/`?data=`/`?openService=` continuam (openService passa a apontar a `/agenda/servicos`). _Modifica: `Agenda.tsx:2890-2901`; `notificationTarget` (`booking/reminder`); e2e agenda/agenda-pagamentos._
- [x] **T2.8 Varredura de deep-links órfãos**: grep final a `?tab=|?vista=|/despesas` em `src/**` + e2e + emails/push (API `notifyUser` payloads) — tudo ou migrado ou coberto por redirect. _Novo teste unit: mapa de redirects._ Resultado: nenhum produtor órfão encontrado (as 7 migrações da Fase 2 já tinham fechado tudo) — só comentários/testes de redirect deliberados; API não tem links diretos para paths com tabs (só `/admin?google=`, já coberto pelo `AdminEntry`, e `/faturacao`/`/setup-password`/`/login`, páginas sem submenu). Novo `tests/unit/legacyRedirects.test.ts` (8 casos) cobre `resolveLegacyTabTarget`.

## Fase 3 — Perfil do tenant (API primeiro; independente das Fases 1-2)

- [x] **T3.1 API: `GET/PUT /users/me` (self)**: novo controller self (padrão `settings/languages` — `req.user`, nunca id no body): GET devolve `{name,email,phone,logoUrl,uiTheme,defaultLanguage}`; PUT atualiza `name,email,phone,logoUrl` (email: exige `currentPassword` + unique — 409; v1 sem verificação por link). Migração `Users.uiTheme` (enum `light|dark|system`, default `system`) + `logoUrl`. Zod + `@swagger` + testes (isolamento self, email duplicado, mass-assignment de `permissionId` rejeitado) + `FUNCIONALIDADES.md`. _Gate: agente seguranca-api; escrita atrás do `billingGate`._ **BUILT** (API-FullStack): `controllers/backoffice/admin/meController.ts` + rotas em `routes/backoffice/admin/userRoutes.ts` (`GET/PUT /users/me`, antes das rotas `/:userId`) + migração `20260706120000-add-uitheme-logourl-to-users.js` + `models/user.ts` + `tests/backoffice/usersMe.test.ts` (20/20 verdes). GET/PUT ambos atrás de `authenticateToken+billingGate` (mesmo mount de `settings/languages`; o gate só bloqueia métodos mutating, por isso o GET não é afetado). Mudança de email exige `currentPassword` só quando o valor MUDA de facto (verificado no handler, não no zod) — reenviar o mesmo email é no-op sem password. Mass-assignment: zod em modo "strip" (sem `.strict()`), mesmo padrão do `gym/memberController.ts`.
- [x] **T3.2 API: `PUT /users/me/password`**: atual→nova (política mínima igual ao setup), bump `tokenVersion` (invalida os outros dispositivos, mantém a sessão atual emitindo novo par de tokens). Testes: password errada 401, sessões antigas invalidadas. _Gate: seguranca-api + Workflow adversarial (código de auth)._ **BUILT** (API-FullStack): `changeMyPassword` em `controllers/backoffice/admin/meController.ts` + rota `PUT /users/me/password` (`authenticateToken+meRateLimit+billingGate`, mesmo trio do `/me`) em `routes/backoffice/admin/userRoutes.ts`. Zod `{currentPassword, newPassword(min 8)}` — mesma política do `setupPassword` legado/registo de clientes, não inventada. Conta sem `password` (signup self-serve por confirmar) → 400 "usa o link de definição de password" (não 401/403 — não há hash para comparar). `currentPassword` errada → 401. Sucesso: `user.tokenVersion++` + novo hash na MESMA `user.save()` (1 UPDATE), depois `revokeAllRefreshTokens({userId})` (fecha os refresh tokens de outros dispositivos na BD) e reemite já a seguir um par novo para o pedido atual — `generateAccessToken`+`createRefreshToken`+cookie `rufvision-refresh-token` (`ADMIN_REFRESH_COOKIE`, agora exportado de `userController.ts`), byte-a-byte o mesmo mecanismo do `/login`. Resposta: `{accessToken}` (mirror do `/refresh`, não do `/login` — não repete name/email que não mudam). Testes novos `tests/backoffice/usersMePassword.test.ts` (17 casos: validação zod, sem password, wrong current, isolamento, e o fluxo completo login→mudar→refresh antigo 401→access antigo 401→sessão nova 200). `FUNCIONALIDADES.md` atualizado (Parte 1 + Parte 2).
- [x] **T3.3 BO: página `/perfil` + menu do avatar**: dropdown no `Avatar` do topbar (`Shell.tsx:145`) → Perfil · Terminar sessão; página com Cards: Conta (nome/email/phone, GuardButton), Password, Preferências (tema light/dark/system com `Tabs` sm + língua padrão reusando `useSettingsLanguages`), Logo (FileUpload `deferred`). `pnpm kubb:refresh` primeiro (hooks `useGetUsersMe`/`usePutUsersMe`/…). `AuthContext` passa a guardar `email`. Rota core `/perfil` fora da sidebar. _Novo: `src/pages/Perfil.tsx`; unit tests da página; e2e perfil._ **BUILT**: spec.json regenerado offline (`dumpSpec.ts`) + `KUBB_OFFLINE=1 pnpm kubb` → hooks `useGetUsersMe`/`usePutUsersMe`/`usePutUsersMePassword`. `src/pages/Perfil.tsx` (novo) com 4 `Card`: Conta (`GuardButton`, campo condicional "Password atual (para mudar o email)" só quando o email muda), Password (`Button` normal — a API não gate este endpoint pelo billing de propósito), Preferências (tema `Tabs` sm com persistência imediata + reversão otimista no erro; língua padrão reusando `useGetSettingsLanguages`/`usePutSettingsLanguages`), Logótipo (`FileUpload` `deferred`, módulo `"profile"`, + colar URL). `AuthContext` (`src/context/AuthContext.tsx`) ganhou `email` (alimentado por `loginRes.email` no login; refresh só herda do localStorage, o JWT não traz email) + dois métodos novos: **`setAccessToken`** (adota o accessToken devolvido por `PUT /users/me/password` sem esperar por um 401→refresh reativo, e realinha `scheduleRefresh`) e **`updateIdentity`** (sincroniza `username`/`email` em memória+localStorage depois de um `PUT /users/me` bem-sucedido, sem precisar de logout/login). `Shell.tsx`: `AvatarMenu` (portal, mesmo padrão do flyout de `NavItemGroup`) substitui o `Avatar` decorativo do topbar — "O meu perfil" + "Terminar sessão"; `/perfil` entra em `guardRoots` (como `/despesas`) e ganha uma entrada em `ROUTE_META` só para o título do topbar (não aparece na sidebar). Rota em `App.tsx`. Testes: `tests/unit/Perfil.test.tsx` (15 casos) + `tests/e2e/perfil.spec.ts` (3 casos, incl. restauro do nome do `admin@e2e` num `finally` — é também o username de login partilhado por quase todos os outros specs). `pnpm exec tsc --noEmit` 0 erros · `pnpm test:unit` 276/276 · `playwright test perfil.spec.ts rbac.spec.ts` 11/11.
- [x] **T3.4 Tema server-side**: `uiTheme` do `GET /users/me` alimenta o init do tema (ordem: servidor > localStorage > sistema); mudar tema (topbar ou perfil) faz PUT + atualiza localStorage; multi-browser resolvido. _Depende: T0.1 + T3.1 + T3.3._ **BUILT**: novo `src/hooks/useThemeSync.ts` — fonte única do tema aplicado, único consumidor de `App.tsx` (substitui o `useState`/`toggleTheme` locais, sem mudar a prop `theme`/`onToggleTheme` que `Shell`/`Login`/`Signup`/`SetupPassword` já recebiam). Init lazy continua local (`computeInitialTheme`, T0.1 — sem sessão ainda não há como saber o servidor, evita esperar rede no 1.º paint); assim que `GET /users/me` resolve (`enabled: isAuthenticated`), um efeito aplica `resolveThemeFromServer(uiTheme, systemPrefersDark)` (nova função pura em `src/lib/uiTheme.ts`) — `"light"`/`"dark"` do servidor ganham sempre (mesmo a um localStorage/sistema diferentes neste browser — a correção do bug original) e ficam gravados no localStorage (fecha o ciclo: o PRÓXIMO reload deste browser já bate com o servidor via o anti-flash do `index.html`); `"system"` segue o sistema atual e **limpa** o localStorage (`clearStoredTheme`, nova função) em vez de gravar a string `"system"` (o anti-flash só conhece light/dark). `toggleTheme` (ícone do topbar, sempre light/dark) aplica localmente + localStorage de imediato e, só se autenticado, grava no servidor (`PUT /users/me {uiTheme}`) com reversão otimista + toast se falhar; sem sessão (Login/Signup/SetupPassword) fica só local, como antes. **Sem contexto dedicado nem duplicação de lógica**: o `PreferenciasCard` do `/perfil` (T3.3) já gravava via `usePutUsersMe` + `invalidateQueries(getUsersMeQueryKey())` — como `useThemeSync` lê a MESMA query key, o React Query entrega o valor novo ao `App.tsx` através da cache partilhada, por isso o toggle do topbar e as 3 opções do Perfil nunca divergem sem precisar de um contexto próprio. **Sem loop**: reaplicar o mesmo valor (ex.: depois do próprio `toggleTheme` invalidar a query) é idempotente. **Cuidado StrictMode**: `toggleTheme` usa `theme` capturado por closure em vez do updater funcional do `setTheme`, porque o React StrictMode invoca updaters funcionais 2× em dev para apanhar impurezas — evita duplicar o PUT. Testes: `tests/unit/uiTheme.test.ts` (+6, `resolveThemeFromServer`), novo `tests/unit/useThemeSync.test.tsx` (6 casos: precedência servidor/localStorage/sistema, toggle com/sem sessão, reversão no erro), `tests/unit/App.test.tsx` (mocks novos de `useGetUsersMe`/`usePutUsersMe` + `QueryClientProvider`, sem mudar as asserções existentes). `pnpm exec tsc --noEmit` 0 erros · `pnpm test:unit` 288/288 · `playwright test perfil.spec.ts` 4/4 + `dashboard.spec.ts` (inclui o teste pré-existente do toggle de tema) 5/5.

## Fase 4 — Polish & fecho

- [ ] **T4.1 Responsivo/a11y do menu novo**: navegação por teclado no acordeão/flyout (setas, Enter, Esc), `aria-current` nos subitens, foco visível, drawer mobile com submenus utilizável a uma mão. Breakpoints: `<lg` drawer, `lg+` sidebar/colapsada.
- [ ] **T4.2 Docs**: CLAUDE.md do BO (tabela de páginas → nova coluna de rotas/submenus, secção do perfil e tema), CLAUDE.md da API (endpoints self), memória.
- [ ] **Design review**: /design-review contra o brief (sidebar expandida/colapsada/mobile, perfil, tema nos 2 modos).

## Ordem de execução recomendada

`T0.1 + T0.2` (paralelo, commits imediatos) → `T1.1` → `T1.2` (gate visual) → `T2.1…T2.7` (paralelo por agentes, worktrees) → `T2.8` → `T3.1 → T3.2 → (kubb) → T3.3 → T3.4` (pode correr em paralelo com a Fase 2 até ao T3.3) → `T4.*`.
