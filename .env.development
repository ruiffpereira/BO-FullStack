# Valores de DEV (carregados só por `pnpm dev` / mode development).
# Produção NÃO lê este ficheiro: as envs têm de ser build-time variables no
# Coolify, senão o build falha (guard no vite.config.ts — sem defaults).
VITE_API_BASE_URL=http://localhost:3001/api
VITE_SITE_ROOT_URL=http://localhost:3000
